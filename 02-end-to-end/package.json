{
    "name": "coding-interview-platform-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && uv run uvicorn main:app --reload --port 3001\" --names \"FRONTEND,BACKEND\" --prefix-colors \"cyan,magenta\"",
        "install:all": "npm install --prefix frontend && cd backend && uv sync",
        "test": "npm test --prefix frontend && cd backend && uv run pytest"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}